# RTX 4090 (Ada Lovelace, compute 8.9) → instale o PyTorch CUDA 12.x PRIMEIRO,
# fora deste arquivo, para garantir o índice cu121 (as wheels cu121 trazem o runtime
# CUDA; basta o driver NVIDIA, sem conda nem CUDA toolkit do sistema).
#
# Espaço de usuário, sem sudo (ver §13.1 da documentação):
#   curl -LsSf https://astral.sh/uv/install.sh | sh && source ~/.local/bin/env
#   uv venv --python 3.11 .venv && source .venv/bin/activate
#   uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   uv pip install -r requirements.txt
#
# Verifique: python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"

nemo_toolkit[asr]>=1.23
librosa>=0.10
soundfile>=0.12
scipy>=1.11
numpy>=1.24
jiwer>=3.0
PyYAML>=6.0
